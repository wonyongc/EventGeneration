import model VLL
define tp = lp lp~
define vtp = vlp vlp~
define boson = w+ w- z h
define muon = mu+ mu- vm vm~
generate p p > tp tp, (tp > boson muon)
add process p p > vtp vtp, (vtp > boson muon)
add process p p > tp vtp, (tp > boson muon), (vtp > boson muon)
output VLLD_mu_M1400 -nojpeg
